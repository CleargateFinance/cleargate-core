// Command worker runs background processing: settlement batching, counterparty
// scoring, agent baselining and the outbox relay.
package main

func main() {
	// TODO(scaffold): app.BuildWorker(cfg).Run(ctx)
}
