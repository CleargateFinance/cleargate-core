// Command api runs the Cleargate HTTP API — the hot path, under a 100ms
// decision budget.
package main

func main() {
	// TODO(scaffold):
	//   cfg  := config.Load()
	//   eng  := app.BuildAPI(cfg)
	//   srv  := &http.Server{Addr: cfg.Addr, Handler: eng}
	//   go srv.ListenAndServe()
	//   <-signals(); srv.Shutdown(ctx)   // graceful drain: never cut a payment
}
